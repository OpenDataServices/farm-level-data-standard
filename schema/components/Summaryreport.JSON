{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "properties": {
    "summary": {
      "description": "This data entity provides metadata about the first mile farm data",
      "primarykey": [
        "exportDate",
        "dataProvidersGeolocation"
      ],
      "properties": {
        "#Persons": {
          "description": "Number of persons in the dataset",
          "minimum": 0,
          "title": "# of Persons",
          "type": "integer"
        },
        "#cropObservations": {
          "description": "Number of crop observation records in the dataset",
          "minimum": 0,
          "title": "# of crop observations",
          "type": "integer"
        },
        "#farmers": {
          "description": "Number of farmers in the dataset",
          "minimum": 0,
          "title": "# of farmers",
          "type": "integer"
        },
        "#farms": {
          "description": "Number of farms in the dataset",
          "minimum": 0,
          "title": "# of farms",
          "type": "integer"
        },
        "#groups": {
          "description": "Number of farmer group recordsin the dataset",
          "minimum": 0,
          "title": "# of groups",
          "type": "integer"
        },
        "#households": {
          "description": "Number of household records in the dataset",
          "minimum": 0,
          "title": "# of Households",
          "type": "integer"
        },
        "#plots": {
          "description": "Number of plotrecords in the dataset",
          "minimum": 0,
          "title": "# of plots",
          "type": "integer"
        },
        "additionalDataAttributes": {
          "description": "Array of additional data attributes added to the data model by customization",
          "items": [
            {
              "$ref": "#/definitions/dataattributestructure"
            }
          ],
          "title": "Additional data attributes",
          "type": "array"
        },
        "additionalDataEntities": {
          "description": "Array of additional data entities added to the data model by customization",
          "items": [
            {
              "$ref": "#/definitions/dataEntityStructure"
            }
          ],
          "title": "Additional data entities",
          "type": "array"
        },
        "dataProvidersAdress": {
          "$ref": "#/definitions/address",
          "description": "The legally registered address of the organisation that converted it's data into First Mile Data Format.",
          "title": "data providers lega; adress"
        },
        "dataProvidersGeolocation": {
          "$ref": "#/definitions/position",
          "description": "Location of the frontdoor of head office of the organisation that converted it's data into First Mile Export Format indicated by WGS84 Lat-Long Coordinates ddd.dddd",
          "title": "Geolocation of the data provider"
        },
        "dataProvidersID": {
          "description": "Legal registration number of the organisation that converted its data into First Mile Data Format",
          "title": "Legal registration number of the data provider",
          "type": "string"
        },
        "dataProvidersName": {
          "description": "Official name of the organisation that converted its data into First Mile Data Format",
          "title": "data provider name",
          "type": "string"
        },
        "dataProvidersPointofContact": {
          "$ref": "#/definitions/contactpoint",
          "description": "Contact point of the organisation that converted it's data into First Mile Export Format",
          "title": "data provider point of contact"
        },
        "datasetDescription": {
          "description": "Description of the data set that is converted into First Mile Export Format",
          "title": "Dataset description",
          "type": "string"
        },
        "exportDate": {
          "description": "Date when the data has been exported from the database into First Mile Export Format",
          "format": "date-time",
          "title": "Export date",
          "type": "string"
        }
      },
      "required": [
        "exportDate",
        "dataProvidersGeolocation",
        "dataProvidersID",
        "dataProvidersName",
        "datasetDescription"
      ],
      "title": "Summary report",
      "type": "object"
    }
  }
}